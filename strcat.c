void strcat(char *str, char *dest)
{
    int i;
    int j;

    i = 0;
    j = 0;
    while(str[j])
    {
        str[j];
    }
    while(dest[i] != '\0')
    {
        str[j] = str[i];
    }
    return (str);
}